import { colors } from '../../utils/theme';

const styles = {
  mainContainer: {
    color: colors.black,
    alignItems: 'center',
    backgroundColor: colors.winterGreen,
    minHeight: 200,
    height: 'auto',
    marginTop: 10,
    width: '200px',
    borderRadius: 13,
    padding: 10,
  },
}

export default styles;